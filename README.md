# Documentation

**First, add it to reference. Then,**
```cs
using PulseAPI;
```
on the top of your code.

Where you want to inject, do
```cs
API.Inject();
```
(case-sensitive)

To execute code,
```cs
API.Execute(string here, it may be textbox.value or anything else.);
```

kthxbai
